<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_add"
        android:icon="@drawable/ic_add"
        android:title="Add Nutrition Record"
        app:showAsAction="never" />
    <item
        android:id="@+id/action_history"
        android:icon="@drawable/ic_list"
        android:title="View Nutrition History"
        app:showAsAction="never" />
    <item
        android:id="@+id/action_stats"
        android:icon="@drawable/ic_stats"
        android:title="Personal statistics"
        app:showAsAction="never" />

    <item
        android:id="@+id/sub_menu"
        android:title="Settings"
        app:showAsAction="never">

        <menu>
            <item
                android:id="@+id/action_edit_info"
                android:icon="@drawable/ic_edit_info"
                android:title="Edit personal information"
                app:showAsAction="never" />
            <item
                android:id="@+id/action_logout"
                android:icon="@drawable/ic_logout"
                android:title="Logout"
                app:showAsAction="never" />
        </menu>
    </item>

</menu>